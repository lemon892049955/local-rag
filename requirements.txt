# Web Framework
fastapi==0.115.6
uvicorn==0.34.0

# HTTP & Scraping
requests==2.32.3
httpx==0.28.1              # 异步 HTTP 客户端（替代 requests 的 async 场景）
beautifulsoup4==4.12.3
readability-lxml==0.8.1
lxml==5.3.0
lxml_html_clean==0.4.1

# LLM Clients
openai==1.58.1          # Kimi/DeepSeek 都兼容 OpenAI 接口

# Vector DB & Embedding
chromadb==0.5.23
sentence-transformers==3.3.1

# Markdown / YAML
pyyaml==6.0.2

# Utilities
python-dotenv==1.0.1
shortuuid==1.0.13
pycryptodome==3.21.0   # 企微消息加解密
python-multipart==0.0.9  # FastAPI 文件上传
jieba==0.42.1            # 中文分词（BM25 检索）

# Multimodal (v0.4)
pymupdf==1.24.0         # PDF 解析，~15MB，纯 CPU
pydub==0.25.1           # 音频分段 (需系统安装 ffmpeg)
